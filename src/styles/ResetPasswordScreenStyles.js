import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent:"flex-start",
    margin:16
  },
  errorText: {
    color: 'red',
    textAlign: 'center',
    marginBottom: 16,
  },
  loader: {
    marginTop: 10,
  },
});
