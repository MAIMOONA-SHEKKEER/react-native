import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "flex-start",
    margin: 20,
    padding: 20,
  },
  errorText: {
    color: "red",
    textAlign: "center",
    marginBottom: 16,
  },
  loader: {
    marginTop: 10,
  },
});
